// tailwind.config.js
module.exports = {
    darkMode: 'class', // ← required for dark mode toggle to work
    content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
    theme: {
        extend: {},
    },
    plugins: [],
}